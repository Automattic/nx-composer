{
	"$schema": "http://json-schema.org/schema",
	"type": "object",
	"cli": "nx",
	"properties": {
		"composerPath": {
			"description": "The path to a composer executable that we can pass commands to. Defaults to global 'composer'.",
			"type": "string",
			"default": "composer"
		},
		"command": {
			"description": "The command to pass to execute.",
			"type": "string"
		},
		"options": {
			"description": "The options to pass to the command.",
			"type": "array",
			"items": {
				"type": "string"
			},
			"default": []
		}
	},
	"required": [ "command" ]
  }
